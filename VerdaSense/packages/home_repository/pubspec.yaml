name: home_repository
description: The Repo that handles page
publish_to: "none"
version: 0.0.1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  equatable: ^2.0.5

  wound_repository:
    path: ../wound_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true